pipeline {
    agent any
         
        stage('Docker Build') {
            steps {
                // Build the Docker image
                
                sh 'docker ps'
            }
        }
        
      }
}
