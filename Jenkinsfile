pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'Jenkinsfile'
            }
        }
        stage('Run') {
            steps {
                sh 'Jenkinsfile'
            }
        }
    }
}
