pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t myimage/myapp:2.0 .
docker images'
            }
        }
        stage('Run') {
            steps {
                sh 'docker build -t myimage/myapp:2.0 .
docker images'
            }
        }
    }
}
